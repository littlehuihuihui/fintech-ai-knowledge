#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""补充数据仓库：OLAP引擎、指标体系"""

import json, re

def make_leaf(node_id, title, level, content):
    return {"id": node_id, "title": title, "level": level, "content": content, "children": []}

leaf_olap = make_leaf("dwh-olap-engines", "OLAP 引擎", "??", """### 课前

- **场景**：数据仓库建好分层模型后，需要选一个查询引擎来跑海量数据的多维分析，不同引擎适用场景不同。
- **目标**：掌握主流 OLAP 引擎（Hive/ClickHouse/Doris/StarRocks/Impala/Presto）的特点、适用场景、选型对比。
- **先修**：数据仓库分层、维度建模

### 是什么

- **一句话定义**：OLAP（Online Analytical Processing）引擎是专门面向海量数据分析查询的计算引擎，支持多维聚合、复杂 JOIN、即席查询，是数据仓库的查询层核心。
- **两大架构**：
  - **MPP（大规模并行处理）**：ClickHouse、Doris、StarRocks、Impala、Greenplum——数据分片存储，多节点并行计算，查询延迟低（秒级/亚秒级）
  - **批处理（MapReduce/Tez/Spark）**：Hive——容错性好，适合超大规模离线计算，但延迟高（分钟级）

### 主流引擎对比

| 引擎 | 架构 | 存储 | 查询延迟 | 适用场景 | 优势 | 劣势 |
|---|---|---|---|---|---|---|
| **Hive** | 批处理（MR/Tez/Spark） | HDFS | 分钟级 | 超大规模离线 ETL、历史数据计算 | 生态成熟、容错好、成本低 | 延迟高、不适合即席查询 |
| **ClickHouse** | MPP（列式） | 本地磁盘 | 亚秒~秒级 | 日志分析、用户行为、时序数据、大宽表 | 查询极快、压缩比高、单表性能强 | JOIN 弱、不支持事务、并发有限 |
| **Doris** | MPP（列式） | 本地磁盘/HDFS | 亚秒~秒级 | 多维分析、实时数仓、用户画像 | 兼容 MySQL 协议、JOIN 好、易运维 | 生态不如 Hive 成熟 |
| **StarRocks** | MPP（列式） | 本地磁盘/对象存储 | 亚秒级 | 实时数仓、多维分析、湖仓一体 | 性能极强、支持外表、向量执行 | 较新、社区规模中等 |
| **Impala** | MPP | HDFS/HBase | 秒级 | Hadoop 生态即席查询 | 与 Hive 元数据共享、查询快 | 内存要求高、容错一般 |
| **Presto/Trino** | MPP（联邦查询） | 不存储（查询多源） | 秒级 | 多数据源联邦查询、数据湖查询 | 跨源查询、连接器丰富 | 不存储数据、超大数据慢 |
| **Druid** | MPP（时序列式） | 本地磁盘 | 亚秒级 | 时序数据、实时监控、OLAP | 实时摄入、时序查询极快 | 不支持 JOIN、适用面窄 |

### 怎么选

```text
你的场景是什么？
│
├── 超大规模离线 ETL（TB~PB，不要求实时）
│   └── 选 Hive（成本低、生态成熟、容错好）
│
├── 即席多维分析（秒级响应，复杂 JOIN）
│   ├── 需要 MySQL 兼容、易运维 → Doris
│   ├── 追求极致性能、实时数仓 → StarRocks
│   └── 单表大宽表、日志分析 → ClickHouse
│
├── 已有 Hadoop 集群，想加速查询
│   └── 选 Impala（共享 Hive 元数据）或 Presto（联邦查询）
│
├── 实时时序数据监控
│   └── 选 Druid（实时摄入+时序查询）
│
└── 多数据源联邦查询（跨 MySQL/Hive/ES/Kafka）
    └── 选 Presto/Trino（连接器丰富）
```

### 企业常见架构组合

```text
# 经典 Lambda 架构：
#   批处理层：Hive（T+1 离线计算）
#   速度层：Flink + Kafka（实时计算）
#   服务层：Doris/ClickHouse（查询加速，合并批流结果）

# 现代实时数仓：
#   Kafka（数据接入）→ Flink（实时 ETL）→ Doris/StarRocks（存储+查询）
#   全链路秒级延迟，支持实时多维分析

# 湖仓一体：
#   数据湖（Iceberg/Hudi/Delta Lake on S3/HDFS）
#     → StarRocks/Presto（直接查询湖数据，无需导入）
#   存算分离，弹性扩展，成本低
```

### 易错对照

| 错法 | 现象 | 纠正 |
|---|---|---|
| 用 Hive 跑即席查询 | 等几分钟才出结果，体验差 | 即席查询用 MPP 引擎（Doris/ClickHouse） |
| 用 ClickHouse 做复杂多表 JOIN | 查询慢甚至 OOM | ClickHouse 适合大宽表单表查询，多表 JOIN 用 Doris/StarRocks |
| 所有数据都导入 MPP 引擎 | 存储成本高 | 冷数据放 Hive/数据湖，热数据放 MPP 引擎 |
| 不考虑并发量选引擎 | 高并发下 ClickHouse 不稳定 | 高并发多维分析选 Doris/StarRocks，ClickHouse 并发有限 |
| 以为 OLAP 引擎支持事务 | 批量更新/删除困难 | OLAP 引擎大多不支持事务，适合追加写入，更新用批量方式 |

### 动手

1. 对比 Hive 和 ClickHouse 对同一张 1 亿行表的 GROUP BY 查询性能
2. 画一个你理解的企业数据仓库查询层架构图，标注每个引擎的位置和作用
3. 写一句「如果让你给一个日增 10 亿条日志的公司选 OLAP 引擎，你会选什么，为什么」
""")

leaf_metrics = make_leaf("dwh-metrics-system", "指标体系", "??", """### 课前

- **场景**：业务方问"今天的活跃用户是多少"，不同人给的答案不一样，因为口径不统一，需要建立统一的指标体系。
- **目标**：掌握指标体系的设计方法（原子指标/派生指标/复合指标）、指标命名规范、指标管理流程、OneData 方法论。
- **先修**：数据仓库分层、维度建模

### 是什么

- **一句话定义**：指标体系是对业务指标的统一定义、命名、计算和管理，确保全公司对同一个指标有一致的理解和口径，是数据驱动决策的基础。
- **核心问题**：指标口径不一致（同名不同义、同义不同名）、指标重复建设、指标无法追溯、业务方看不懂。
- **OneData 方法论**（阿里提出）：统一指标定义、统一数据分层、统一维度管理、统一数据服务。

### 指标分类

```text
指标体系
│
├── 原子指标（原子度量）
│   定义：不可再拆分的最基础指标，通常是对某个字段的聚合
│   公式：聚合函数(度量字段)
│   示例：
│     - 支付金额 = SUM(订单.支付金额)
│     - 订单数 = COUNT(订单.订单ID)
│     - 活跃用户数 = COUNT(DISTINCT 用户.用户ID)
│   特点：没有业务修饰，最纯粹的聚合
│
├── 派生指标
│   定义：原子指标 + 业务修饰（时间周期 + 统计维度 + 业务限定）
│   公式：原子指标 + 修饰词
│   示例：
│     - 昨日华东地区支付金额 = 支付金额(原子) + 昨日(时间) + 华东(维度)
│     - 近7天移动端活跃用户数 = 活跃用户数(原子) + 近7天(时间) + 移动端(限定)
│     - 当月VIP用户订单数 = 订单数(原子) + 当月(时间) + VIP(限定)
│   特点：有明确的业务含义，是业务方最常用的指标
│
└── 复合指标
    定义：由多个原子/派生指标计算得到的指标
    公式：指标A 运算符 指标B
    示例：
      - 客单价 = 支付金额 / 订单数
      - 转化率 = 下单用户数 / 访问用户数
      - 复购率 = 复购用户数 / 总购买用户数
      - 毛利率 = (收入 - 成本) / 收入
      - 环比增长率 = (本期 - 上期) / 上期
    特点：有明确的业务含义，通常是比率/百分比
```

### 指标命名规范

```text
# 命名公式：时间周期 + 业务限定 + 统计维度 + 原子指标
# 示例：
#   昨日 华东 支付金额
#   近7天 移动端 活跃用户数
#   当月 VIP 客单价

# 命名原则：
# 1. 见名知意：看到名字就知道指标的含义和口径
# 2. 统一前缀：时间周期放最前面
# 3. 避免缩写：不用业务方看不懂的缩写
# 4. 不重复：同一指标只有一个名字，同一名字只有一个指标

# 反例（不好的命名）：
#   - "用户数"（什么用户？活跃还是注册？什么时间？）
#   - "GMV"（业务方可能不知道是 Gross Merchandise Volume）
#   - "转化率"（什么转化率？访问→下单？还是浏览→加购？）
#   - "昨日数据"（太笼统，不知道具体指标）
```

### 指标管理流程

```text
# 1. 指标需求提出
#    业务方提出指标需求，说明业务场景、计算口径、使用频率
#
# 2. 指标定义评审
#    数据团队与业务方对齐口径：
#    - 原子指标是什么？
#    - 时间周期是什么？（自然日/滚动7天/当月）
#    - 业务限定是什么？（是否排除测试订单/退款订单）
#    - 统计维度是什么？（按地区/渠道/产品）
#    - 数据来源是哪张表？
#
# 3. 指标开发
#    数据工程师在数仓中开发指标：
#    - 原子指标通常在 DWS 层（汇总层）
#    - 派生指标通过维度组合得到
#    - 复合指标通过指标计算得到
#    - 编写指标文档（定义、口径、SQL、负责人）
#
# 4. 指标验证
#    - 与业务方手工计算的结果对比
#    - 与历史数据趋势对比
#    - 边界情况验证（空值、零值、异常值）
#
# 5. 指标发布
#    - 录入指标管理平台（如 Apache Atlas、DataWorks、自研平台）
#    - 配置指标血缘（上游表、下游报表）
#    - 设置指标负责人和告警
#
# 6. 指标运维
#    - 监控指标数据质量（波动、缺失、异常）
#    - 指标口径变更走评审流程
#    - 定期清理无用指标
```

### 指标维度管理

```text
# 维度是分析指标的角度，统一维度管理是指标体系的重要部分

# 常用维度：
# - 时间维度：年/季/月/周/日/时，自然周期/滚动周期
# - 地域维度：国家/省/市/区，大区/城市
# - 产品维度：品类/品牌/SKU，产品线/产品
# - 渠道维度：线上/线下，APP/小程序/PC/第三方
# - 用户维度：新用户/老用户，VIP/普通，年龄段/性别
# - 终端维度：iOS/Android/Web，手机型号/操作系统版本

# 一致性维度（Conformed Dimension）：
#   同一个维度在不同的指标和数据集市中定义一致
#   例如："华东地区"在销售指标和用户指标中包含的城市完全相同
#   这是 Kimball 维度建模的核心原则之一
```

### 易错对照

| 错法 | 现象 | 纠正 |
|---|---|---|
| 指标口径不写清楚 | 不同人理解不同，数据对不上 | 每个指标必须有明确的定义文档，包含计算公式和业务限定 |
| 同名不同义 | "活跃用户"在不同报表口径不同 | 建立统一指标字典，全公司共用一套指标定义 |
| 同义不同名 | "支付金额"和"GMV"其实是一个东西 | 统一命名规范，一个指标只有一个标准名称 |
| 复合指标直接在报表层计算 | 口径不一致，重复计算 | 复合指标在数仓层统一计算，报表直接取 |
| 指标没有负责人 | 出问题没人管 | 每个指标指定业务负责人和技术负责人 |
| 不做指标血缘 | 指标出错不知道影响哪些报表 | 建立指标血缘，上游变更可追溯下游影响 |

### 动手

1. 选一个你熟悉的业务（如电商），设计 5 个原子指标、5 个派生指标、3 个复合指标
2. 给"日活跃用户数"写一个完整的指标定义（包含口径、计算公式、业务限定、数据来源）
3. 画一个指标体系的分层结构图（原子指标→派生指标→复合指标→业务报表）
""")

# ========== 读取并修改 embed-dwh.js ==========
with open(r"D:\cursor\数据学习平台\kg-data\embed-dwh.js", "r", encoding="utf-8") as f:
    content = f.read()

match = re.search(r'window\.__KG_EMBEDDED\["dwh"\]\s*=\s*(\{.*\})\s*;?\s*$', content, re.DOTALL)
data = json.loads(match.group(1))

# 在根节点添加 OLAP 引擎和指标体系
root_children = data["children"]
existing_ids = [c["id"] for c in root_children]

if "dwh-olap-engines" not in existing_ids:
    root_children.append(leaf_olap)
    print("✓ OLAP 引擎已添加")

if "dwh-metrics-system" not in existing_ids:
    root_children.append(leaf_metrics)
    print("✓ 指标体系已添加")

# 序列化
new_json_str = json.dumps(data, ensure_ascii=False, separators=(',', ':'))
new_content = f'window.__KG_EMBEDDED = window.__KG_EMBEDDED || {{}};\nwindow.__KG_EMBEDDED["dwh"]={new_json_str}\n'

with open(r"D:\cursor\数据学习平台\kg-data\embed-dwh.js", "w", encoding="utf-8") as f:
    f.write(new_content)

print(f"\nDone! embed-dwh.js size: {len(new_content)} bytes")
